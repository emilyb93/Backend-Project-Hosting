const {formatData, insertData} = require("../db/utils/data-manipulation")
const testData = require('../db/data/test-data/index.js');

console.log(testData)
const 

// describe('#formatData', () => {
//     test('should change the data given, into an array of arrays', () => {
        // expect(formatData)
//     });
// });